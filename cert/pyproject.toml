[project]
name = "forgetmenot-cert"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "jinja2>=3.1",
  "weasyprint>=62",        # HTML → PDF
  "cryptography>=42",      # Ed25519 signing/verifying
  "pydantic>=2.7",
]
